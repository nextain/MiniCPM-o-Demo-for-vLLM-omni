[pytest]
markers =
    phase1: cross-phase test against demo gateway (HF transformers)
    phase2: cross-phase test against vllm-omni /v1/realtime
    phase3: cross-phase test via naia-os ts-client wrapper
asyncio_mode = auto
testpaths = tests
